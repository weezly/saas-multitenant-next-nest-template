# PostgreSQL Configuration for Docker
DB_USER=saas_user
DB_PASSWORD=saas_password_dev
DB_NAME=saas_db
DB_PORT=5432

# Backend Configuration
NODE_ENV=development
SERVICE_PORT=3001
JWT_SECRET=dev-secret-key-min-32-characters-change-this-in-production
JWT_EXPIRES_IN=7d

# Frontend Configuration
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001
